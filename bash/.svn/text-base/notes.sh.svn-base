#!/bin/bash

#  .
# ..: philip chang, university of illinois at urbana-champaign

function _sync_note() {
  gnsync --path ./ --notebook $(basename $PWD) --two-way TWO_WAY --format markdown --mask *.markdown
}
